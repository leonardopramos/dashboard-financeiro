spring:
  config:
    activate:
      on-profile: docker

gateway:
  cors:
    allowed-origins: ${GATEWAY_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8080,https://app.tccleonardopreczevski.com.br,https://dashboard-financeiro-frontend-production-9e02.up.railway.app/}
